The project demostrates:
  1. Inclusion of some configuration nodes sub-tree into existing configuration.
  2. Inclusion of some configuration nodes sub-tree (from external file) into existing configuration.
  3. Merging of two configurations. And different types of nodes merging are shown.